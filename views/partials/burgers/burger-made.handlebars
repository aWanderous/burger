<li class="list-group-item">
	{{burger_name}}
</br>
	<button class="change-devoured btn btn-secondary btn-sm btn-block" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#unless devoured}}Devour!{{/unless}}
	</button>
</li>
