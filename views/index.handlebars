<h1>Eat-Da-Burger!</h1>

<h2>Burgers to eat.</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{burgers devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers eaten.</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{burgers devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger's Name:</label>
    <input type="text" id="bun" name="burger_name">
  </div>

  <button type="submit">Make burger</button>
</form>
